# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Opis projektu

Strona internetowa dla firmy BlockchainWares - software development company z Dąbrowy Górniczej, specjalizującej się w blockchain, EDA, engineering i bazach danych.

## Komendy

```bash
npm run dev                        # Dev server na localhost:4321
npm run build                      # Build produkcyjny do .vercel/output/ (adapter @astrojs/vercel)
npm run preview                    # Podgląd builda
npm test                           # Playwright E2E, 253 testy (chromium 247 + chromium-no-js 6)
node scripts/hash_password.mjs     # Generuje ADMIN_PASSWORD_HASH z hasła podanego interaktywnie
npx tsc --noEmit                   # Type check
```

## Tech Stack

- **Astro 5** w trybie **SSR** (`output: "server"`, `@astrojs/vercel@9` adapter) + **React 19** (client:load dla interaktywnych sekcji)
- **Tailwind CSS 4** z **DaisyUI 5** (custom theme "blockchainwares")
- **Framer Motion 12** (animacje sekcji)
- **lucide-react** (ikony faktów wydarzenia, `src/components/event-icons.tsx` — jedyny dotychczasowy konsument tej zależności w repo)
- **TypeScript** (strict mode via astro/tsconfigs/strict)

## Deploy

Vercel, autodeploy z gita. Build wychodzi do `.vercel/output/` (adapter `@astrojs/vercel`), nie `dist/server/entry.mjs`.

`Dockerfile` i `compose.yml` zostają w repo jako alternatywa dla VPS, ale są NIESPÓJNE z obecnym adapterem — `CMD`/`HEALTHCHECK` wskazują na `dist/server/entry.mjs`, który przy `@astrojs/vercel` w ogóle nie powstaje (Dockerfile ma na to komentarz ostrzegawczy). Powrót na VPS wymaga najpierw cofnięcia adaptera na `@astrojs/node`.

## Architektura

Full-stack SSR (Astro + React Islands). **Żadna strona marketingowa nie jest prerenderowana** — jedyny prerender w repo jest na `/404`. Wydarzenia migrowały z lokalnej stałej na moduł w `elysia-backend` (patrz niżej), a status `ongoing/upcoming/past` musi liczyć się na żywo na każdej z nich (banerze, liście, detalu), więc `index.astro` i `markets.astro` straciły `prerender = true`, które miały wcześniej — inaczej lista wydarzeń zamroziłaby się na dniu builda. Middleware **wykonuje się teraz dla wszystkich trzech** (`/`, `/markets`, `/markets/<id>`) i ustawia im nagłówki bezpieczeństwa; `vercel.json` (reguła 1, negative lookahead na `/api` i `/admin`) ustawia te same wartości **równolegle**, nie zamiennie — zostaje świadomie, bo to jedyne źródło, które dosięga prerenderowanego `/404`, plików statycznych i dokłada HSTS (middleware nie chodzi po żadnym z nich). Oba źródła muszą zgadzać się co do wartości: `tests/security_headers.spec.ts` porównuje `src/lib/net/security_headers.ts` z `vercel.json` i wywala się na rozjeździe.

- `src/layouts/Layout.astro` - bazowy layout landingu z SEO, Open Graph, fontami (Quicksand). Opcjonalny prop `canonicalPath` (podaje go trasa SSR `markets/[id].astro` jako `get_event_path(event)`, bo `trailingSlash: "ignore"` dawał dwa indeksowalne URL-e na jedno wydarzenie); wszystkie 4 bloki JSON-LD idą przez `to_json_ld` (spójne escapowanie)
- `src/layouts/AdminLayout.astro` - osobny layout panelu admina (noindex, font systemowy, `rounded-md`)
- `src/pages/index.astro`, `src/pages/markets.astro` - strony publiczne (SSR), importują sekcje z `src/components/`. Odczyt wydarzeń idzie przez `load_events()` (patrz `src/lib/events/` niżej); `load_events()` rzuca tylko na pustym cache — awarię API tłumaczą one na baner/listę pustą lub nieaktualną, nigdy na 500
- `src/pages/markets/[id].astro` - podstrona pojedynczego wydarzenia (SSR). Nieznany `id` → prawdziwy status **404** renderowany przez samą trasę, nie przez `Astro.rewrite("/404")` — Astro rzuca `ForbiddenRewrite` przy próbie rewrite na prerenderowane `/404`. **Milczące API wydarzeń daje 503, nie 404** (z `Retry-After`) — brak odpowiedzi nie jest dowodem, że wydarzenia nie ma; te dwie sytuacje mają inny kod celowo. Treść strony jest w `EventDetail.tsx`
- `src/components/EventDetail.tsx` - treść strony detalu wydarzenia, renderowana **bez** `client:*` (delta JS trasy 0 kB). Wydzielona z trasy `markets/[id].astro`, bo jednoplikowa wersja miała 546 linii przy limicie 500. Lista „Other events" mieszka osobno w `RelatedEvents.tsx` z tego samego powodu; `EventHours.tsx` renderuje godziny wspólnie dla karty, banera i detalu. Prop `container` (domyślnie `main`) istnieje dla podglądu w panelu — tam komponent stoi już wewnątrz `<main>` panelu, a drugi taki landmark byłby niepoprawny
- `src/components/event-theme.ts` - tokeny prezentacji wyciągnięte z `EventCard.tsx` (`STATUS_THEME`, `WORKSHOP_LABEL`, `get_event_link`, `format_admission`, `get_event_hours`, `get_venue_note`, klasy badge/pill), żeby karta i detal nie rozjechały się wizualnie
- `src/components/event-types.ts` - kontrakt wydarzenia, **dzielony verbatim z modułem `blockchain-wares` w repo `backend-api`** — pole dodane tylko po jednej stronie znika u drugiej po cichu, więc każda zmiana kształtu jest zmianą w dwóch repo, nie jednym. Trzyma `TradeFairEvent` i typy zagnieżdżone, `IconKey`/`ICON_KEYS` (zamknięty preset 12 kluczy kebab-case dla `facts.icon` — klucz jest daną, nie nazwą komponentu, więc zmiana biblioteki ikon nigdy nie dotyka zapisanych wydarzeń ani walidatora backendu; klucze są _append-only_, zmiana nazwy zeruje żywe rekordy) i limity `MAX_EVENT_BADGES`/`MAX_EVENT_FACTS`/`MAX_EVENT_LINKS` (4/6/6 — limity układu, mirrorowane przez walidator backendu, nie pułapy zapisu z innego powodu). `MAX_EVENT_TOPICS` (8) jest inny: to pułap wyłącznie UI, `TradeFairEvent.topics` w kontrakcie go nie ma. Trzy nowe pola listowe (`badges`, `facts`, `links`) i `venue.note` są czysto prezentacyjne — żadne z nich nie trafia do JSON-LD (patrz pułapka przy `event-schema.ts` niżej)
- `src/components/event-icons.tsx` - `EVENT_ICONS`, mapowanie `IconKey -> LucideIcon` **wyczerpujące po typie** — dopisanie klucza do `ICON_KEYS` bez wpisu tutaj wywala build zamiast renderować pustkę. `EventIcon` ujednolica grubość kreski (14px, stroke 2px) z resztą rysowanych ręcznie elementów karty
- `src/components/EventBadges.tsx`, `EventFacts.tsx`, `EventLinks.tsx` - renderery trzech nowych list; każdy **nie rysuje nic, gdy lista jest pusta** — wywołujący zachowuje własny layout zamiast dziury po pustym bloku. `EventBadges` (etykiety nad tytułem) stoi na banerze, karcie i detalu; `EventFacts` (ikona z presetu + tekst) na karcie i detalu; `EventLinks` **wyłącznie na detalu** — miejsca na karcie i banerze nie starczyłoby na jeszcze jedną listę. `EventLinks` filtruje adresy przez `to_safe_href()`, drugi guard poza walidacją zapisu: schemat inny niż `http`/`https` albo wartość, której `URL` nie zbuduje, po prostu nie renderuje się jako link, zamiast trafić do `href`
- `src/components/` - sekcje strony jako React komponenty z Framer Motion
- `src/components/ui/` - reużywalne komponenty UI (Button, etc.)
- `src/components/our-works-data.ts` - dane sekcji "What We Do" + deep-linki do tabów: `?docs`, `?sdk`, `?core`, `?hive`, `?ufa`, `?eos`, `?eda`, `?data` (i `?tab=<alias>`). Duplikat aliasu **wywala build** celowo (`build_id_by_slug()`). Deep-link zatrzymuje auto-rotate i scrolluje do `#what-we-do-content` (treść kategorii pod navbarem), nie do nagłówka sekcji
- `src/components/events-data.ts` - **czyste helpery nad modelem wydarzenia, bez własnych deklaracji typu** — kontrakt (`TradeFairEvent` i cała reszta) przeniósł się do `event-types.ts` wyżej; ten plik go re-eksportuje, żeby żaden dotychczasowy `from "./events-data"` się nie wysypał. `get_event_by_id()`, `get_event_path()`, `MARKETS_PATH`, `get_event_status()`, `group_events_by_status()`, `get_promoted_events()`, formattery (`format_venue_address`, `get_venue_map_url`, ...; teksty widoczne dla czytelnika są w `event-theme.ts`) — konsumuje je też `event-schema.ts` przy budowaniu JSON-LD. `kind: "workshop"` przełącza teksty z „jedziemy” na „prowadzimy”, `schedule` dokłada godziny do `dateTime`/JSON-LD i domyka status o godzinie startu oraz końca (o północy dnia warsztatu jest jeszcze „upcoming”); **każde pole grupy zagnieżdżonej jest niezależne** — sama godzina otwarcia domyka tylko start („from 10:00 CEST”), sam `organizer.name` renderuje się bez linku, `venue` bez adresu — bez dojazdu, `venue` daje adres na karcie i w `PostalAddress`, `admission` — pigułkę „Free entry · no registration”, `Offer` (`price: "0"`) wskazujący domyślnie na `/markets` (na detalu — na własny `canonical_path`) i `isAccessibleForFree`. `EventBanner` promuje **do dwóch** najbliższych wydarzeń z `get_promoted_events()` (trwające przed nadchodzącymi, `limit` domyślnie 2), nie jedno
  - **Pułapka**: link do mapy powstaje z adresu pocztowego (`get_venue_map_url()`), nie z nazwy venue — pod tą samą nazwą stoi obok hotel, więc wyszukiwanie po nazwie trafia w złe miejsce. Ulica **bez miasta** też nie daje linku (szukałaby się po całym świecie), więc `hasMap` i „Venue & directions” znikają razem
  - **Pułapka**: niepełna grupa nie trafia do JSON-LD na siłę — `Offer` powstaje dopiero z pary cena+waluta (samo `price: "0"` zostaje jako `isAccessibleForFree`), a `Organization` wymaga nazwy; niepoprawnego bloku lepiej nie emitować
  - **Pułapka**: baner i `Markets` liczą status z `parse_iso_day(todayIso)` na pierwszym renderze i dopiero po mount przechodzą na `new Date()` — inaczej render serwera i hydracja klienta rozjeżdżają się na granicy doby
  - **Pułapka**: `get_event_status()` liczy dobę **w strefie wydarzenia** (offset z `TradeFairEvent.utcOffset`, `schedule.utcOffset` wygrywa jeśli jest, fallback = strefa renderera). Bez tego trasa SSR (zegar serwera = UTC na Vercelu) rozjeżdżała się ze statusem na liście o do 2h wokół północy CEST
- `src/lib/events/` - moduł wydarzeń, konsument `elysia-backend` (endpoint `/events`, moduł `blockchain-wares` w backendzie). `source.ts` — klient odczytu: cache w pamięci procesu (TTL `EVENTS_API_TTL_SECONDS`), stale-fallback (nieudane odświeżenie oddaje ostatnie znane dane zamiast 500) i 20 s cooldown po błędzie, żeby ruch nie płacił pełnego timeoutu na każdym renderze — wzorowany na `src/lib/logs/source.ts`. `mutations.ts` — klient zapisu (POST/PATCH/DELETE, nagłówek `X-API-Key`, `redirect: "manual"` żeby klucz nie poleciał na obcy host po przekierowaniu); typy `EventPatch`/`EventDraft`, `MAX_BODY_BYTES` i `serialize()`/`byte_length()` mieszkają dziś w wydzielonym `mutation_payload.ts` (odseparowany, gdy `mutations.ts` zaczął ocierać się o limit 500 linii) — `EventPatch` wyprowadza zestaw pól kasowalnych `null`-em wprost z `TradeFairEvent` (typ mapowany `OptionalEventKey`), więc nowe pole opcjonalne jak `badges` czy `venue.note` wchodzi do niego samo, bez edycji tego pliku. `parse_event.ts` — bramka kształtu: rekord bez wymaganych pól wypada z listy zamiast wywalać render (wyjątek: `image` jest kosmetyczny — zły albo niekonstruowalny adres zdejmuje samo pole, bo backend waliduje zapis samym wzorcem i wpuszcza np. `http://[`, a zniknięcie rekordu ukryłoby go także w panelu); to samo dziś obejmuje `badges`/`facts`/`links` — zły element listy zdejmuje siebie, nie cały rekord, i tnie do `MAX_EVENT_BADGES`/`FACTS`/`LINKS` z zachowaniem kolejności. `source.ts` dokłada nad tym dedup po `id` (drugi wpis tego samego slugu też odpada, zamiast wywalić `get_event_by_id()` na `/markets/<id>`). `form_mapping.ts` — mapowanie formularza panelu na `TradeFairEvent`/patch: katalog stały ma 29 pól + checkbox rejestracji (`EVENT_FORM_FIELDS`), do tego `venue.note` i trzy grupy powtarzalne adresowane osobno (`SlotFormField`: do `MAX_EVENT_BADGES`/`FACTS`/`LINKS` slotów jako `badges.<i>`, `facts.<i>.icon|label`, `links.<i>.label|url`; `topics` zostaje jednym polem katalogu, bo jedzie jako lista po przecinku) — razem do 59 nazw pól w jednym formularzu. `form_parsers.ts` — prymitywy odczytu wspólne dla obu kierunków (`text()`, `absolute_url()`, `offset()`, `checkbox()`, `read_topics()`): typ `Slot<T>` rozróżnia cztery stany (`absent`/`clear`/`invalid`/`value`), na czym stoi cała semantyka „pole nieobecne = bez zmian, puste = kasuj”. `admin_actions.ts` — akcje stron `/admin/events/*`, dziś cieńszy o wydzielony `admin_form_state.ts` (`field_errors`/`request_rejection`/`body_rejection`/`preserve_submitted_values`, ten sam powód co `mutation_payload.ts` wyżej)
  - **Pułapka**: puste pole opcjonalne w formularzu edycji znaczy **skasuj** (`null` → backend robi `$unset`), dlatego strona edycji musi prefillować przez `load_events({ force: true })` — prefill z nieświeżego cache'u cicho cofnąłby poprzednią edycję, której ta instancja procesu jeszcze nie widziała
  - **Pułapka**: cache jest per-instancja procesu (jak logi nginx) — przy zimnym starcie na Vercelu i jednoczesnej awarii API nie ma z czego wyrenderować listy. Świadomie odrzucono trzymanie fallbacku wydarzeń w repo
  - **Pułapka Cloudflare (rozwiązana, 2026-09)**: podczas wdrożenia produkcja dostawała 403 na każdym renderze mimo poprawnego backendu i poprawnych zmiennych środowiskowych — Cloudflare przed `api.bard-dev.com` odrzuca ruch server-side z Vercela po bot score/ASN, jeszcze zanim dotrze do Traefika (Traefik go w ogóle nie logował). `blockchain-wares` jest pierwszym projektem robiącym fetch do tego backendu **z serwera** (pozostałe konsumują go z przeglądarki użytkownika), więc ta ścieżka nigdy wcześniej nie była testowana. Próba z jawnym `User-Agent` (domyślny był `node`) okazała się **nieskuteczna** — blokada działa po reputacji IP, nie po nagłówku. Obejście: `EVENTS_API_URL` wskazuje na **`api-direct.bard-dev.com`**, osobny host z rekordem DNS A na ten sam VPS ale z **wyłączonym proxy Cloudflare** (DNS only) i osobnym routerem w Traefiku na tę samą usługę — nie zmieniaj tego z powrotem na `api.bard-dev.com`, produkcja wtedy padnie. Szczegóły i checklist dla kolejnych projektów: pattern KB `cloudflare-blocks-vercel-ssr-fetch-to-own-api`
  - **Pułapka**: pusty slot grupy powtarzalnej przy edycji kasuje pole tak samo jak reszta formularza — `read_repeated()` (`form_mapping.ts`) zwraca „kasuj” dla listy bez ani jednej wartości. `topics` ma **inną semantykę**: `read_topics()` (`form_parsers.ts`) i `read_topics()` w `parse_event.ts` traktują pustą tablicę jako legalny stan szkicu (`[]`, „bez tematów”), podczas gdy `read_badges()`/`read_facts()`/`read_links()` w `parse_event.ts` odrzucają pustą tablicę jako `undefined` — backend nigdy nie persystuje `[]` dla tych trzech. Backendowy hook kasowania (`backend-api`, moduł `blockchain-wares`, `model.ts`) nie pokrywa operatorów modyfikujących zawartość tablicy (`$pull`/`$pop`/`$pullAll`) — udokumentowane tam wprost, bo zapis pól listowych idzie wyłącznie przez `.create()`/`$set`/`$unset`
  - **Pułapka**: niepełna para w slocie dwupolowym (fakt bez ikony, link bez adresu) to **błąd walidacji** `required` na brakującym polu pary, nie ciche pominięcie — inaczej zapis wyglądałby na udany, a wpisana treść (etykieta faktu, nazwa linku) po prostu by zniknęła. Slot całkowicie pusty jest pomijany normalnie
- `src/pages/admin/events/` - CRUD panelu: `index.astro` (lista + wiek danych + ostrzeżenie o nieświeżości), `new.astro`, `[id]/edit.astro`, `[id]/delete.astro` (potwierdzenie kasowania jako osobna strona, nie `confirm()`). Każda strona **POST-uje na samą siebie** i przy odrzuceniu renderuje ten sam formularz ponownie z wpisanymi wartościami — **nie** robi redirectu (formularz ma dziś kilkadziesiąt adresowalnych pól, patrz `form_mapping.ts` wyżej — nie zmieściłyby się w URL-u, a bez JS nie ma jak ich odtworzyć). Redirect 303 zostaje wyłącznie dla sukcesu. `new.astro` i `[id]/edit.astro` układają formularz i `EventPreview` (`client:load`) **obok siebie** w siatce (`[data-editor-grid]`: formularz w lewej kolumnie, podgląd w prawej i `lg:sticky`; poniżej `lg` jedna kolumna, w kolejności DOM podgląd nad formularzem) i podają podglądowi `initialFields` — prefill albo wartości odrzuconego zapisu, żeby pierwszy render nie zaczynał od pustego szkicu. Bez JS-a siatka wraca do bloku przez własny `<noscript>` na stronie (`[data-editor-grid]` → `display: block`, plus chowa skrót „Przejdź do formularza”, `[data-skip-form]`) — osobno od `<noscript>` w `AdminLayout.astro`, który chowa sam podgląd (`[data-event-preview]`)
- `src/components/event-schema.ts` - `build_event_schema(event, site, canonical_path?)` ma trzeci parametr opcjonalny; lista `/markets` dalej ma `offers.url` na `/markets`, detal wydarzenia ma kanoniczny URL wydarzenia. Pole `image` jest opcjonalne w `EventSchema` (schema.org je i tak traktuje jako zalecane, nie wymagane) — brak go zamiast `new URL()` rzucającego na złym adresie. `badges`, `facts`, `links` i `venue.note` **nigdy nie trafiają do schematu** — świadomie: schema.org nie ma pola na dowolne etykiety prezentacyjne, a `sameAs` nazywa profile tej samej encji (np. własny profil na Facebooku), nie dowolne linki tematyczne, więc wepchnięcie tam `links` przekłamałoby dane. Pilnuje tego `tests/event_schema.spec.ts` (test na wyciek unikalnych etykiet do serializowanego JSON-LD)
- `src/components/events-meta.ts` - `build_markets_description(events, now)` liczy meta description `/markets` z najbliższych wydarzeń zamiast z zaszytego na sztywno tekstu, z twardym limitem 160 znaków i fallbackiem na ogólny opis firmy (pusta lista albo zimny cache po awarii API). Opis był ostatnim miejscem z zamrożonym zestawem wydarzeń po migracji na API — bez tego strona wymagałaby deployu przy każdej zmianie wydarzeń, dokładnie tego, co migracja miała usunąć
- `astro.config.mjs` - bez `sitemap.customPages`: wydarzenia przychodzą z API dopiero w runtime, więc `@astrojs/sitemap` (który emituje tylko strony prerenderowane w chwili builda) nie ma jak ich zobaczyć. Trasy `/markets/<id>` wystawia własny endpoint `src/pages/sitemap-events.xml.ts` (zgłoszony osobno w `public/robots.txt`), którego `serialize` w konfiguracji integracji `sitemap` zdejmuje końcowy slash — inaczej sitemap wskazywałaby inny URL niż `canonicalPath` strony (`trailingSlash: "ignore"`)
- `src/pages/sitemap-events.xml.ts` - milczące API wydarzeń daje **503** (z `Retry-After`), nie pusty `<urlset>` z 200 — pusta sitemapa powiedziałaby robotom, że `/markets/<id>` można wyindeksować, a to stan przejściowy, który przeżyłby samą awarię
- `src/components/Navigation.tsx`, `src/components/Footer.tsx` - aktywny link dopasowywany **prefiksowo** (`/` matchuje tylko siebie) — inaczej „Markets” tracił `aria-current` na podstronie wydarzenia. Wejścia na detal: tytuł wydarzenia w `EventCard` i każdy wpis w `EventBanner` (baner promuje do dwóch wydarzeń, więc dawny jeden zbiorczy `<a href="/markets">` został rozbity na osobne linki na wpis; CTA banera dalej idzie na `/markets`)
- `src/components/admin/` - dashboard panelu (kafelki, wykres, top-N, tabela z sortowaniem/filtrowaniem/paginacją i deep-linkami przez query string), renderowany serwerowo. `TrafficFilters.tsx` + `SwitchLink.tsx` renderują 7 przełączników ruchu (polaryzacja jednolita: switch ON = kategoria widoczna, w URL zapisana jako odwrócona flaga `exclude*`/`includeInternal`). `CountryCard.tsx` doklada `showCountryIps` (top IP per kraj, prezentacyjne — nie zeruje `page`, w odróżnieniu od pozostałych 6 flag ruchu)
- **Edytor wydarzenia** (`EventForm.tsx` + `event_form_groups.ts` + `EventFieldSlots.tsx` + `EventFormAdvanced.tsx` + `EventFormField.tsx`) - jedna karta w dziesięciu wierszach zamiast dawnej płaskiej listy sześciu grup. `event_form_groups.ts` przegrupowuje katalog stały z `event_form_fields.ts` (który zostaje bez zmian — `GROUPS` dalej karmi wyczerpujący typ `UndeclaredEventFormField`, a chodzi po nim też `tests/support/admin_events.ts`) na `PRIMARY_ROWS` (widoczne od razu: termin, wyróżniki, tytuł, miejsce, godziny, lokalizacja, treść, fakty, tematy, linki — cztery z tych wierszy to grupy powtarzalne) i `ADVANCED_GROUPS` (strefy i format czasu, wstęp, organizator, metadane — pod natywnym `<details data-advanced>`, licznik „n z X wypełnionych” w `<summary>`, otwiera się już na serwerze, gdy błąd trafia w pole schowane w środku). `EventFormField.tsx` niesie części wspólne (`FormField`, `GroupLegend`) między kartą, sekcją Zaawansowane i slotami, żeby żaden z trzech modułów nie importował pozostałych i nie powstał cykl
  - **Grupy powtarzalne** (`badges`/`facts`/`links`/`topics`) renderuje `EventFieldSlots.tsx` (`SlotGroup`/`SlotItem`) jako z góry wyrenderowane sloty do limitu grupy (`MAX_EVENT_BADGES`/`FACTS`/`LINKS`/`TOPICS`), każdy zawsze obecny w DOM i w wysyłce — progressive enhancement: bez JS wszystkie sloty są widoczne i zapisywalne. `src/scripts/admin_slots.ts` chowa nadmiarowe puste sloty, odsłania przyciski „+”/„×” i trzyma w synchronizacji licznik oraz kolor ramki, ale **nigdy nie tworzy, nie usuwa ani nie przestawia węzłów** — indeksy `badges.<i>`, `facts.<i>.icon|label`, `links.<i>.label|url` muszą zostać dokładnie takie, jak wyrenderował serwer
    - **Pułapka**: sloty sterowane są wyłącznie atrybutem `hidden`, nigdy `disabled` — pole z `disabled` wypadłoby z `FormData` przy wysyłce i rozjechałoby parę `facts.<i>.icon`/`facts.<i>.label` albo `links.<i>.label`/`links.<i>.url`
    - **Pułapka**: niepełna para w slocie (fakt bez ikony, link bez adresu) to błąd walidacji `required`, nie ciche pominięcie; slot całkowicie pusty jest pomijany normalnie, a pusty **przy edycji kasuje pole** — patrz pułapki `topics` kontra `badges`/`facts`/`links` przy `src/lib/events/` wyżej
  - `src/components/admin/read_event_form.ts` re-eksportuje `read_event_form()` z `form_mapping.ts` i trzyma `collect_fields()` (zbiera `FormData` formularza do `URLSearchParams`) — podgląd **celowo nie ma własnego mapowania**: próba osobnego mappera została odrzucona w review, bo rozjeżdżała się z zapisem po pierwszej zmianie pola
- `EventPreview.tsx` to **jedyna wyspa panelu** (`client:load`): podgląd wydarzenia w trzech wariantach (baner / karta / strona), składany z DOM formularza (`EVENT_FORM_ID`) przez `read_event_form()` z modułu wyżej — ten sam mapper, którym idzie zapis. Renderują go prawdziwe `EventBanner`/`EventCard`/`EventDetail`, nie atrapy; jedyny prop spoza formularza to `related={[]}` (powiązane wydarzenia pochodzą z listy, nie z formularza). Scena jest `inert` + `pointer-events-none`: podgląd nie zabiera fokusu polom formularza (dziś kilkadziesiąt, licząc sloty) i nie wyprowadza z niego kliknięciem w link do wydarzenia. Wartość niepoprawna w trakcie pisania (`https://`, kod kraju) zostawia ostatni złożony stan zamiast znikać co drugi znak. Wpis bez daty i wpis miniony wypadają z banera przez `get_promoted_events()`, więc wariant „mini” pokazuje wtedy zdanie wyjaśniające, a nie puste miejsce
- `src/lib/utils.ts` - helper `cn()` do mergowania klas Tailwind
- `src/middleware.ts` - na requestach do stron dynamicznych: guard sesji dla `/admin/*` i `/api/admin/*`, nagłówki bezpieczeństwa. Nie loguje już ruchu
- `src/lib/logs/` - dane ruchu pochodzą ze **zdalnego pliku logu nginx** (`LOG_SOURCE_URL`), pobieranego serwerowo, cache'owanego w pamięci (`source.ts`, TTL `LOG_SOURCE_TTL_SECONDS`) i parsowanego (`nginx_parser.ts`, format `combined` + dwa dodatkowe cytowane pola: X-Forwarded-For i Accept-Language). `LogSourceRepository` jest **READ-ONLY** (bez `insert`) — własne logowanie w middleware, mocki i implementacja in-memory zostały usunięte
  - **Pułapka**: pole `ip` bierze się z `$remote_addr`, NIE z X-Forwarded-For — ten nagłówek jest sterowalny przez klienta
  - `user_agent.ts` kategoryzuje ruch nie-ludzki na 4 rodziny botów (`BotCategory`: crawler/seo/script/headless, pole `RequestLog.botCategory`) plus UA nierozpoznany i brak nagłówka UA — razem 6 niezależnych flag URL (`excludeCrawlers`, `excludeSeoTools`, `excludeScripts`, `excludeHeadless`, `excludeUnknownUa`, `excludeNoUa`) zamiast dawnego jednego `excludeBots`. Stary `excludeBots=1` nadal jest parsowany jako alias wszystkich sześciu i przekierowuje (303) na URL kanoniczny — zapisane linki nie psują się
  - **Pułapka**: klasyfikacja UA i predykat filtra muszą czytać z jednego źródła (`is_missing_user_agent()` w `user_agent.ts`) — dwie osobne kopie tego warunku się rozjeżdżały (pusty string wpadał do `unknownUa` zamiast `noUa`)
  - Ruch z loopbacku (health-checki, ~40% wpisów) to jeden z 7 przełączników w bloku "Filtry ruchu" (`includeInternal`), nie samotna flaga
  - Panel mieli **cały** log, nie okno najnowszych wpisów. `RECORD_BUDGET` w `nginx_parser.ts` (500 000 rekordów, ~300 MB) to zabezpieczenie przed OOM, nie limit prezentacji: po jego wyczerpaniu parser zostawia najnowsze wpisy (czyta plik od końca), zwraca `truncated` i panel dokleja adnotację "Widoczna jest tylko najnowsza część historii: X z Y linii logu". Budżet jest parametrem `parse_nginx_log()` — testy wstrzykują małą wartość zamiast hodować fixture
  - **Pułapka**: `FETCH_TIMEOUT_MS` (60 s) i `maxDuration` adaptera w `astro.config.mjs` (90 s) to para — Vercel ubija funkcję po `maxDuration`, więc podniesienie timeoutu fetcha bez tego drugiego jest martwe w produkcji
- `src/lib/net/user_agent.ts` - `SSR_USER_AGENT` (`BlockchainWaresSSR/1.0 (+https://blockchainwares.com.pl)`), współdzielony przez `events/source.ts` i `events/mutations.ts` na każdym żądaniu do modułu wydarzeń. **Nie** rozwiązuje blokady Cloudflare opisanej wyżej (blokada jest po IP, nie po nagłówku) — zostaje jako poprawna identyfikacja klienta w logach backendu. `src/lib/logs/source.ts` dalej leci bez własnego UA
- `src/lib/auth/` - jeden admin, hasło jako hash scrypt w zmiennej środowiskowej, sesja jako podpisane HMAC ciasteczko `bw_session` (TTL 7 dni)
- `src/pages/api/auth/*` - logowanie/wylogowanie, obsługiwane przez natywne formularze POST
- `src/pages/admin/*` - strony panelu (`/admin`, `/admin/login`)

**Panel admina jest czysto serwerowy — to wymóg właściciela, nie detal implementacyjny.** Zero requestów z przeglądarki: dane pobiera frontmatter `src/pages/admin/index.astro`, filtry to `<form method="get">`, sortowanie i paginacja to linki, logowanie/wylogowanie to natywne formularze POST. Endpointy `/api/admin/logs`, `/api/admin/stats` i hook `use_admin_logs` zostały usunięte. CRUD wydarzeń (`/admin/events/*`) trzyma się tej samej zasady: same natywne `<form method="post">`, żadnego `fetch` po stronie panelu mimo tego, że dane teraz żyją w zewnętrznym API — `tests/admin_events.spec.ts` ma wariant bez JS dokładnie z tego powodu. Panel działa przy wyłączonym JavaScripcie — nie dodawać `fetch`/`XHR` przy kolejnych zmianach.

**Jedyny wyjątek: podgląd wydarzenia na `/admin/events/new` i `/admin/events/<id>/edit` — świadoma decyzja właściciela, nie przeoczenie.** Formularz ma od razu pokazywać, jak wpis wygląda w banerze, na karcie i na własnej stronie, a to da się policzyć tylko w przeglądarce, więc `src/components/admin/EventPreview.tsx` jest wyspą React `client:load`. Wyjątek jest wąski i taki ma zostać:

- podgląd czyta **wyłącznie** DOM formularza i **nie wysyła żadnego żądania** — zakaz `fetch`/`XHR` z panelu obowiązuje dalej, pilnują go `tests/admin_events.spec.ts`, `tests/admin_event_preview.spec.ts` i `tests/admin_event_slots.spec.ts`;
- zapis pozostaje natywnym `<form method="post">` na tę samą stronę. Bez JavaScriptu formularz zapisuje się tak samo — traci się sam podgląd. Żadne pole ani przycisk zapisu nie może zależeć od skryptu;
- `<noscript>` w `AdminLayout.astro` chowa `[data-event-preview]`, żeby bez JS nie zostawał martwy przełącznik wariantów. Sam atrybut niesie stan hydracji (`static` przed nią, `live` po), bo przed hydracją przyciski są w DOM, ale jeszcze nie działają;
- `PreviewFont.astro` doklada do `<head>` font landingu (Quicksand) przez slot `head` — panel chodzi na foncie systemowym i bez tego podgląd pokazywałby właściwy układ w niewłaściwym kroju.

Sekcje strony głównej (w kolejności): Navigation, Hero, OurWorks (What We Do + Portfolio), Services, EndUsers, Team, Career, Contact, Footer

## Zmienne środowiskowe

Wymagane w runtime (nie na etapie builda) — bez nich `get_auth_config()`/`get_log_source_url()`/`get_events_api_url()`/`get_events_api_key()` rzuca i aplikacja nie startuje:

- `ADMIN_PASSWORD_HASH` - hash hasła admina, generowany przez `node scripts/hash_password.mjs`
- `AUTH_SECRET` - sekret do podpisu ciasteczka sesji, min. 32 znaki
- `LOG_SOURCE_URL` - URL zdalnego logu nginx. Ścieżka zawiera token — to sekret, nigdy w kodzie/repo
- `EVENTS_API_URL` - bazowy adres modułu wydarzeń w `elysia-backend` (bez końcowego ukośnika — patrz `src/lib/env.ts`). Wskazuje na host **omijający Cloudflare** (`api-direct.bard-dev.com`, DNS only), nie na `api.bard-dev.com` — Cloudflare blokuje (403) server-side fetch z Vercela do tego drugiego po bot score/ASN. Nie "poprawiać" z powrotem na `api.bard-dev.com`, patrz pułapka w `src/lib/events/` wyżej i pattern KB `cloudflare-blocks-vercel-ssr-fetch-to-own-api`
- `EVENTS_API_KEY` - klucz serwisowy do zapisu wydarzeń (`X-API-Key`). Odczyt listy jest publiczny i klucza nie używa; mutacje bez niego kończą się `misconfigured`, nie próbą zapisu

Opcjonalne:

- `LOG_SOURCE_TTL_SECONDS` - jak długo pobrany log żyje w cache procesu, w sekundach (domyślnie 300)
- `EVENTS_API_TTL_SECONDS` - jak długo odpowiedź modułu wydarzeń żyje w cache procesu, w sekundach (domyślnie 300)
- `SESSION_NOT_BEFORE` - kill-switch unieważniający wszystkie sesje bez rotacji `AUTH_SECRET`
- `SITE_ORIGIN` - kanoniczny origin dla guardu CSRF; brak = `site` z `astro.config.mjs`

Nigdy nie wpisywać realnych wartości tych zmiennych do repo/dokumentacji.

## Testy

`npm test` (Playwright, 253 testy: chromium 247 + chromium-no-js 6, w 21 plikach speców) korzysta z lokalnych fixture'ów — logu (`tests/fixtures/`, nie uderza w prawdziwy `LOG_SOURCE_URL`) i modułu wydarzeń (`tests/fixtures/serve_events.mjs`, serwer HTTP ze stanem w pamięci procesu, adres wstrzyknięty jako `EVENTS_API_URL` w `playwright.config.ts` — nie uderza w prawdziwy `elysia-backend`). Projekt `chromium-no-js` weryfikuje, że panel admina (w tym CRUD wydarzeń) **zapisuje** bez JavaScriptu — nie że widać w nim podgląd; wariant bez JS sprawdza wprost, że panel podglądu jest wtedy schowany. Osobny test pilnuje, że z panelu nie wychodzi żaden request XHR/fetch. Specy panelu: `tests/admin.spec.ts` (ogólny przepływ), `tests/admin_traffic.spec.ts` (7 flag filtrów ruchu), `tests/admin_countries.spec.ts` (karta krajów + `showCountryIps`), `tests/admin_events.spec.ts` (pełna ścieżka dodanie/edycja/usunięcie przez listę z potwierdzeniem, odrzucony zapis wraca z wpisanymi wartościami, brak sesji przekierowuje z każdej z 4 tras, zero XHR/fetch z frontu, wariant bez JS), `tests/admin_event_slots.spec.ts` (grupy powtarzalne formularza: „+” odsłania kolejny slot bez wypadania z kolejności Tab, odrzucony zapis oddaje wpisane linki a nie sam formularz, błąd pola ze schowka otwiera „Zaawansowane” już na serwerze, wyczyszczenie wszystkich slotów grupy kasuje pole a sąsiednia grupa zostaje nietknięta, prefill edycji odtwarza wyróżniki/fakty/linki/przypis lokalizacji; wariant bez JS potwierdza, że wszystkie sloty są od razu widoczne i formularz zapisuje się bez skryptu), `tests/admin_event_preview.spec.ts` (podgląd nadąża za nazwą i datą, wszystkie trzy warianty, wpis bez daty tłumaczy brak wariantu w banerze, prefill edycji trafia do podglądu, zero XHR/fetch — **żaden test w tym pliku nie zapisuje**, bo stan fixture'a należy do `admin_events.spec.ts`, a pliki chodzą równolegle na jednym serwerze i `reset_events()` stąd zabrałby tamtemu rekord), `tests/nginx_parser.spec.ts` (budżet rekordów — degradacja na wstrzykniętej małej wartości, bez przeglądarki). Wydarzenia: `tests/events_data.spec.ts` (daty z harmonogramem, granice godziny startu i końca w `get_event_status()`, kształt JSON-LD wraz z `offers`/`isAccessibleForFree`/`PostalAddress`, link do mapy z adresu, kolejność `get_promoted_events()`, `offers.url` z `canonical_path`, guard duplikatu `id` w `get_event_by_id()`, doba w strefie wydarzenia na fixture `TOKYO_CONFERENCE` — na własnych fixture'ach, bez przeglądarki), `tests/events_new_fields.spec.ts` (`parse_event()` dla `badges`/`facts`/`links`/`venue.note`: element bez wymaganego pola albo spoza `ICON_KEYS`/wzorca URL wypada sam, reszta rekordu i reszta listy zostają; nadmiar nad `MAX_EVENT_BADGES`/`FACTS`/`LINKS` ucina się do limitu z zachowaniem kolejności; pusta lista po odsiewie znika jako pole, nie zostaje `[]`; kilka złych elementów naraz nie zdejmuje rekordu; osobny blok renderuje kartę, baner i detal z kompletem nowych pól i bez nich — regresja pilnuje, że wydarzenie bez nich wygląda jak przed migracją), `tests/events_source.spec.ts` (cache/cooldown/dedup pobrań, stale-fallback po awarii API, `parse_event()` — też bez przeglądarki, na podstawionym `fetch` zamiast na sieci), `tests/event_detail.spec.ts` (nawigacja z karty i z banera na `/markets/<id>`, treść detalu, status 404 + noindex, JSON-LD pojedynczego Eventu, wariant no-JS z asercją dokładnie 1 `astro-island`; gałąź 503 przy martwym API wydarzeń nie ma pokrycia E2E — sprawdza ją tylko kod trasy), `tests/events_partial_render.spec.ts` (render karty, banera i detalu przy niepełnych danych — SSR przez Vite, bez przeglądarki: żaden wariant nie zostawia w HTML-u `undefined`, pustego elementu ani osieroconego separatora, a niepełna grupa nie zabiera ze sobą całej sekcji), `tests/events_meta.spec.ts` (`build_markets_description()`: limit 160 znaków, fallback na pustej liście i na zimnym cache po awarii API, dobór wariantu — pełne/skrócone nazwy, jedno/dwa wydarzenia) i `tests/ssr_user_agent.spec.ts` (`SSR_USER_AGENT` na odczycie i każdej mutacji modułu wydarzeń, nagłówek nie podszywa się pod przeglądarkę, origin zgodny z `site` z `astro.config.mjs`, render `/markets` realnie wysyła nagłówek — na fixture'ze `serve_events.mjs`, który wystawia podgląd nagłówków przychodzących żądań). Wspólne locatory i kroki w `tests/support/` (`tests/support/events.ts` dla detalu wydarzenia — w tym `is_event_schema()`, dzielony guard kształtu bloku JSON-LD, przeniesiony tu z `event_detail.spec.ts` żeby nie mieć dwóch kopii; `tests/support/admin_events.ts` dla CRUD panelu, deleguje obsługę grup powtarzalnych do `tests/support/admin_event_slots.ts` — `reveal_slot()`/`parse_slot()`/`fill_topics()`/`expect_topics()`), fixture UA/logów w `tests/fixtures/user_agents.ts` i `tests/fixtures/log_entries.ts`, fixture wydarzeń w `tests/fixtures/events.ts` (`SEED_EVENTS`/`SEED_EVENT_IDS` — jeden seed niesie dziś też badges/facts/links/venue.note, żeby pokryć prefill każdej grupy naraz; `reset_events()` przywraca zestaw startowy przed każdym testem CRUD, bo stan mutacji żyje w pamięci fixture'a), fixture ręcznych kształtów `tests/fixtures/event_shapes.ts` (wydarzenia pisane wprost dla speców kalendarza/JSON-LD, dzielone między nimi, żeby dwie asercje nie mówiły o dwóch różnych wydarzeniach) i serwer testowy `tests/fixtures/serve_events.mjs`, którego PATCH emuluje `$unset` prawdziwego backendu (`null` w patchu kasuje pole zamiast zostawiać je jako `null` w zapisanym rekordzie).

## DaisyUI Theme

Custom theme "blockchainwares" zdefiniowany w `src/styles/global.css` - dark mode z oklch colors. Używaj klas DaisyUI (`bg-base-100`, `text-base-content`, `btn-primary`, etc.).

## Konwencje

- Komponenty React: `PascalCase.tsx`
- Utils: `snake_case.ts`
- TypeScript strict, zero `any`
- Max 500 linii na plik
- Export komponentów przez barrel files (`index.ts`)
